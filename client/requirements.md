## Packages
recharts | For visualizing enrolment and demographic trends
react-leaflet | For the India map visualization
leaflet | Core mapping library
clsx | For conditional class names
tailwind-merge | For merging tailwind classes efficiently

## Notes
The backend is a Python FastAPI app.
Base API URL is /api.
Use space-separated HSL for CSS variables.
